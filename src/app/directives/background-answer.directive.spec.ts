import { BackgroundAnswerDirective } from './background-answer.directive';

describe('BackgroundAnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundAnswerDirective();
    expect(directive).toBeTruthy();
  });
});
